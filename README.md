# forgotten_objects

# Принцип работы
Алгоритм использует две модели фона MOG2 с разной скоростью адаптации:
  Short Background (быстрый фон) — быстро обновляется и выделяет движущиеся объекты.
  Long Background (медленный фон) — обновляется медленно и фиксирует новые объекты в сцене.
